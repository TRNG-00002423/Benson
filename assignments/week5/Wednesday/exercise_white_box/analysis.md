1. Not possible, cart_subtotal >= 35 executes before cart_subtotal >= 50 so it never will execute.
2. Not possible, cart_subtotal >= 35 executes before cart_subtotal >= 50 so it never will execute.
3. Nothing makes it true, it's false for (cart_subtotal: < 35, region: "US", is_member: True)